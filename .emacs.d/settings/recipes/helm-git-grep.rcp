(:name helm-git-grep
       :description "helm for [git grep], an incremental git-grep"
       :type github
       :pkgname "yasuyk/helm-git-grep"
       :depends helm
       :post-init(progn
                     (setq helm-git-grep-max-length-history 300)))
